<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AskMeMobileApp.Views.NewWordPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand">
            <Label Text="Unit" FontSize="Medium" />
            <Entry Text="{Binding Word.Unit}" FontSize="Small" />
            <Label Text="PolishMeaning" FontSize="Medium" />
            <Editor Text="{Binding Word.PolishMeaning}" FontSize="Small" Margin="0" />
            <Label Text="EnglishMeaning" FontSize="Medium" />
            <Editor Text="{Binding Word.EnglishMeaning}" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>